name: Scrape Brussels/EU
on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scrape-brussels
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - run: pip install -r requirements.txt
      - name: Install Playwright if needed
        run: |
          if grep -q "requires_js: true" src/config/sources-brussels.yaml 2>/dev/null; then
            playwright install chromium
          else
            echo "No JS sources, skipping Playwright"
          fi
      - name: Run Brussels scrapers
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m src.cli run --country brussels
        timeout-minutes: 25
      - name: Commit results
        run: |
          git config user.name "Pol-Intel Bot"
          git config user.email "bot@orison.co"
          git add data/jobs.db feeds/
          git diff --staged --quiet || git commit -m "Update Brussels feeds [skip ci]"
          git push
